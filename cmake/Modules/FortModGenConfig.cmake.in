set(FortModGen_VERSION @FortModGen_VERSION@)

@PACKAGE_INIT@

get_filename_component(FortModGen_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
list(APPEND CMAKE_MODULE_PATH "${FortModGen_CMAKE_DIR}")

include(${FortModGen_CMAKE_DIR}/FortModGen)
include(${FortModGen_CMAKE_DIR}/FortModGenTargets.cmake)
